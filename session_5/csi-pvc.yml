apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-volume-claim
spec:
  storageClassName: do-block-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi